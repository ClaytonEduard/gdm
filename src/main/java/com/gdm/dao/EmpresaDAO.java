package com.gdm.dao;

import com.gdm.domain.Empresa;

public class EmpresaDAO extends GenericDAO<Empresa> {

	// @SuppressWarnings("unchecked")
	// public List<Pessoa2> listarOrdenado() {
	// Session sessao = HibernateUtil.getFabricaDeSessoes().openSession();
	// try {
	// String t;
	// t = "J";
	// Criteria consulta = sessao.createCriteria(Pessoa2.class);
	// consulta.createAlias("Pessoa2", "p");
	// consulta.addOrder(Order.asc("p.grupo"));
	// consulta.equals(t);
	//
	// List<Pessoa2> resultado = consulta.list();
	// return resultado;
	// } catch (RuntimeException erro) {
	// throw erro;
	// } finally {
	// sessao.close();
	// }
	// }

}
